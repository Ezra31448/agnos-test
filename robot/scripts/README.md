# Scripts

Place Python scripts here. Examples:

- Data generators or seed scripts
- CI helper scripts (e.g. environment setup)
- One-off utilities used by tests or keywords

Run from project root, e.g. `python robot/scripts/your_script.py`.
